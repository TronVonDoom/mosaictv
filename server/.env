# Local development defaults (NOT secret — safe to commit).
# In Docker/production these are overridden by docker-compose + Dockerfile ENV.
# Used by the Prisma CLI (prisma generate / db push) during local dev.
DATABASE_URL="file:./dev.db"
